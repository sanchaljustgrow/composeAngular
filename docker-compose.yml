services:
  angular-frontend:
    image: sanchaljustgrow/ang-springs-frontend:latest
    container_name: angular-frontend
    ports:
      - "8080:80"
    environment:
      - API_URL=https://example.com/auth/login
